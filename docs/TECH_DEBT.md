# Technical debt register

Visible on purpose. Each item says what the shortcut is, why it was taken, and how to pay it down.

| #   | Item                                                                | Impact                                    | Why it exists                                                                                                                                                                         | How to pay down                                                                                                                           |
| --- | ------------------------------------------------------------------- | ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Worker bundle is ~2.2 MB (608 KB gzip)                              | Slower first render; cold start ~1–2 s    | `@react-email/components` re-exports everything: Tailwind v4 compiler, prettier (unused, `pretty: false`), Prism, html-to-text                                                        | Import only the needed `@react-email/*` packages in `renderTemplate.ts` and expose a hand-built module map; or lazy-load Tailwind support |
| 2   | Main bundle is ~1.2 MB (383 KB gzip)                                | Fine for an internal tool                 | CodeMirror + Radix + fonts in one chunk                                                                                                                                               | Vite `codeSplitting` / dynamic import of the editor and dialogs                                                                           |
| 3   | Two copies of `@react-email/render` may exist                       | Bundle size, type drift                   | `@react-email/components` pins 2.0.6                                                                                                                                                  | Add an npm `overrides` entry when upgrading                                                                                               |
| 4   | Types are stripped, not checked, in the browser                     | Type errors surface as runtime errors     | sucrase does not type-check; TS 7 has no in-browser transpile API                                                                                                                     | Run `tsc` on templates in CI; later add editor diagnostics via a language service in a worker                                             |
| 5   | Regex import pre-check is best-effort                               | Only affects error message quality        | Simplicity                                                                                                                                                                            | Parse with sucrase's parser or `@babel/parser` to list specifiers precisely                                                               |
| 6   | Worker is not a security boundary                                   | Unsafe for untrusted authors              | Internal MVP; documented in ARCHITECTURE.md                                                                                                                                           | Host the worker in a cross-origin iframe, or compile/render on a server sandbox                                                           |
| 7   | Preview CSP allows `img-src http:`                                  | Tracking pixels fire on preview           | Emails often use http images                                                                                                                                                          | Restrict to `https:` or proxy images                                                                                                      |
| 8   | Templates must be single files                                      | No shared partials/styles                 | The require shim only knows three modules                                                                                                                                             | Allow a curated `@studio/shared` module in the map                                                                                        |
| 9   | Sample data and metadata live in `registry.ts`                      | Editing metadata means editing code       | No persistence layer yet                                                                                                                                                              | Move to D1/API in the persistence milestone; keep the `EmailTemplate` shape                                                               |
| 10  | Light theme only; `.dark` tokens defined but no toggle              | None today                                | Not required; no existing app to match                                                                                                                                                | Add a theme toggle (class strategy already configured)                                                                                    |
| 11  | `shadcn` is a runtime dependency                                    | Slightly odd dependency graph             | `index.css` imports `shadcn/tailwind.css` (the CLI wrote it)                                                                                                                          | Leave as is; it is bundled at build time                                                                                                  |
| 12  | E2E runs use port 4173 and reuse an existing server                 | Stale server can be reused locally        | Playwright default                                                                                                                                                                    | Set `reuseExistingServer: false` in CI (already true for CI via env)                                                                      |
| 13  | Payload JSON errors sometimes lack a position                       | Less precise message                      | V8 omits positions for "Unexpected token" errors                                                                                                                                      | The CodeMirror JSON linter underlines the spot; acceptable                                                                                |
| 14  | `hardenWorkerGlobals` list is hand-maintained                       | New browser APIs are not covered          | Simplicity                                                                                                                                                                            | Review the list per browser release; consider deleting everything not on an allow-list                                                    |
| 15  | Send server has no authentication                                   | Any local process can trigger a test send | Loopback-only developer tool                                                                                                                                                          | Add a shared token or session auth before deploying it anywhere                                                                           |
| 16  | Send server keeps its rate limit in memory                          | Resets on restart                         | Simplicity                                                                                                                                                                            | Fine locally; use KV/D1 when deployed as a Worker                                                                                         |
| 17  | Node runs `server/*.ts` via native type stripping                   | Requires Node 22.6+ (flag) or 23.6+       | Avoids a build step and a `tsx` dependency                                                                                                                                            | Add a `tsx`/build step if older Node must be supported                                                                                    |
| 18  | Vite `--host` would expose the send server to the LAN via the proxy | LAN users could trigger test sends        | Proxy forwards requests; server only checks Host/Origin                                                                                                                               | Bind-check the proxy or require a shared token before allowing `--host`                                                                   |
| 19  | Deployed studio has no authentication                               | Anyone with the URL can use the editor    | **Code done**: `server/auth.ts` verifies Access JWTs and every `/api/*` route needs one. Not switched on: the deploy target has no custom domain, so no Access application exists yet | Dashboard steps in `docs/DEPLOYMENT.md` ("Putting Cloudflare Access in front"), then disable the `workers.dev` route                      |
| 20  | Deployed on the developer's personal Cloudflare account             | Bus factor; wrong owner for production    | No company account existed on 2026-09-09; pipeline built first                                                                                                                        | Follow "Moving to the production account" in `docs/DEPLOYMENT.md` before D1 exists                                                        |
| 21  | Worker cannot send email; two local runtimes to explain             | Live sends need the Node path             | The AWS SDK needs Node APIs and would bloat the Worker bundle                                                                                                                         | `aws4fetch` sender behind the same `EmailSender` interface (phase 1); then drop `dev:node`, the proxy and the AWS SDK                     |
| 22  | `tsconfig.worker.json` still includes Node types                    | Node-only code could slip into the Worker | `server/sesSender.ts` (AWS SDK) is type-checked in the same program via `server/app.ts` imports                                                                                       | Remove `"types": ["node"]` once #21 is paid down                                                                                          |
